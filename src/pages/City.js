import React from 'react';
import { Box, Paper } from '@mui/material';
import CityTable from '../components/CityTable';

const City = () => {
    return (
        <Box>
            <Paper>
                <CityTable />
            </Paper>
        </Box>
    );
};

export default City;
