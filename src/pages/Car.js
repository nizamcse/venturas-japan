import React from 'react';
import { Paper } from '@mui/material';
import CarList from '../components/CarList';

const Car = () => {
    return (
        <div>
            <Paper>
                <CarList />
            </Paper>
        </div>
    );
};

export default Car;
